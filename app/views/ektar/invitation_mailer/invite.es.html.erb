<%= I18n.t(
  "mailers.invitation.body_html",
  email: @invitation.email,
  organization_name: @invitation.organization.name,
  invitation_link: accept_invitations_url(@token, **@url_options)
).html_safe %>
